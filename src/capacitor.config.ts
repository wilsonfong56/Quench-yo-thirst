import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Quench Yo Thirst',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
